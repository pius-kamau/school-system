<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= teacher.name %> - Teacher Profile</title>
    <style>
      :root {
        --primary: #ff416c;
        --bg-dark: #1a0033;
        --text-light: #f8fafc;
        --card-bg: #ffffff;
        --body-bg: #f8fafc;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Inter", Arial, sans-serif;
      }

      body {
        background-color: var(--body-bg);
        padding: 20px;
      }

      .profile-container {
        max-width: 1000px;
        margin: 0 auto;
      }

      /* Header */
      .header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 30px;
      }

      .header h1 {
        font-size: 24px;
        font-weight: 600;
        color: #333333;
      }

      .action-buttons {
        display: flex;
        gap: 10px;
      }

      .btn {
        padding: 10px 20px;
        border-radius: 6px;
        text-decoration: none;
        font-weight: 500;
        font-size: 14px;
      }

      .btn-back {
        background: #6b7280;
        color: white;
      }

      .btn-edit {
        background: var(--primary);
        color: white;
      }

      /* Profile Card */
      .profile-card {
        background: var(--card-bg);
        border-radius: 10px;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        overflow: hidden;
        margin-bottom: 30px;
      }

      .profile-header {
        background: var(--primary);
        color: var(--text-light);
        padding: 30px;
        display: flex;
        align-items: center;
        gap: 30px;
      }

      .profile-photo {
        width: 120px;
        height: 120px;
        border-radius: 50%;
        object-fit: cover;
        border: 4px solid rgba(255, 255, 255, 0.3);
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
      }

      .photo-placeholder {
        width: 120px;
        height: 120px;
        border-radius: 50%;
        background: rgba(255, 255, 255, 0.2);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 48px;
        color: white;
        border: 4px solid rgba(255, 255, 255, 0.3);
      }

      .teacher-info h2 {
        font-size: 28px;
        font-weight: 600;
        margin-bottom: 5px;
      }

      .teacher-id {
        background: rgba(255, 255, 255, 0.2);
        padding: 5px 15px;
        border-radius: 20px;
        font-size: 14px;
        display: inline-block;
        margin-bottom: 15px;
      }

      .status-badge {
        padding: 6px 15px;
        border-radius: 20px;
        font-size: 13px;
        font-weight: 500;
        display: inline-block;
      }

      .status-active {
        background: rgba(255, 255, 255, 0.3);
        color: white;
      }

      /* Profile Content */
      .profile-content {
        padding: 30px;
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 30px;
      }

      .info-section h3 {
        color: #333333;
        font-size: 16px;
        font-weight: 600;
        margin-bottom: 20px;
        padding-bottom: 10px;
        border-bottom: 2px solid #f0f0f0;
        display: flex;
        align-items: center;
        gap: 10px;
      }

      .info-item {
        display: flex;
        justify-content: space-between;
        padding: 12px 0;
        border-bottom: 1px solid #f0f0f0;
      }

      .info-item:last-child {
        border-bottom: none;
      }

      .info-label {
        color: #666666;
        font-weight: 500;
      }

      .info-value {
        color: #333333;
        font-weight: 600;
        text-align: right;
      }

      /* Back Link */
      .back-link {
        margin-top: 20px;
        text-align: center;
      }

      .back-link a {
        color: var(--primary);
        text-decoration: none;
        font-weight: 500;
      }

      .back-link a:hover {
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <div class="profile-container">
      <!-- Header -->
      <div class="header">
        <h1>👩‍🏫 Teacher Profile</h1>
        <div class="action-buttons">
          <a href="/teachers" class="btn btn-back">← Back to List</a>
          <a href="/teachers/edit/<%= teacher.id %>" class="btn btn-edit"
            >✏️ Edit Profile</a
          >
        </div>
      </div>

      <!-- Profile Card -->
      <div class="profile-card">
        <!-- Profile Header -->
        <div class="profile-header">
          <% if (teacher.photo) { %>
          <img
            src="<%= teacher.photo %>"
            alt="<%= teacher.name %>"
            class="profile-photo"
          />
          <% } else { %>
          <div class="photo-placeholder">👤</div>
          <% } %>

          <div class="teacher-info">
            <h2><%= teacher.name %></h2>
            <div class="teacher-id">Teacher ID: #<%= teacher.id %></div>
            <div class="status-badge status-active">
              <%= teacher.status || 'Active' %>
            </div>
          </div>
        </div>

        <!-- Profile Content -->
        <div class="profile-content">
          <!-- Personal Information -->
          <div class="info-section">
            <h3>📋 Personal Information</h3>
            <div class="info-item">
              <span class="info-label">Gender:</span>
              <span class="info-value"><%= teacher.gender || 'N/A' %></span>
            </div>
            <div class="info-item">
              <span class="info-label">Date of Birth:</span>
              <span class="info-value"><%= teacher.dob || 'N/A' %></span>
            </div>
            <div class="info-item">
              <span class="info-label">Phone:</span>
              <span class="info-value"><%= teacher.phone || 'N/A' %></span>
            </div>
            <div class="info-item">
              <span class="info-label">Email:</span>
              <span class="info-value"><%= teacher.email || 'N/A' %></span>
            </div>
            <div class="info-item">
              <span class="info-label">Address:</span>
              <span class="info-value"><%= teacher.address || 'N/A' %></span>
            </div>
          </div>

          <!-- Professional Information -->
          <div class="info-section">
            <h3>🎓 Professional Information</h3>
            <div class="info-item">
              <span class="info-label">Subject:</span>
              <span class="info-value"><%= teacher.subject || 'N/A' %></span>
            </div>
            <div class="info-item">
              <span class="info-label">Qualification:</span>
              <span class="info-value"
                ><%= teacher.qualification || 'N/A' %></span
              >
            </div>
            <div class="info-item">
              <span class="info-label">Experience:</span>
              <span class="info-value"><%= teacher.experience || 'N/A' %></span>
            </div>
            <div class="info-item">
              <span class="info-label">Date of Joining:</span>
              <span class="info-value"
                ><%= teacher.date_of_joining || 'N/A' %></span
              >
            </div>
            <div class="info-item">
              <span class="info-label">Salary:</span>
              <span class="info-value">
                <%= teacher.salary ? 'KES ' + teacher.salary : 'N/A' %>
              </span>
            </div>
          </div>
        </div>
      </div>

      <div class="back-link">
        <a href="/teachers">← Back to All Teachers</a>
      </div>
    </div>
  </body>
</html>
