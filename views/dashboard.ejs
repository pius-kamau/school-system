<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dashboard - School Management System</title>
    <style>
      :root {
        --primary: #ff416c;
        --secondary: #ff4b2b;
        --accent: #ff5964;
        --bg-dark: #1a0033;
        --text-light: #f8fafc;
        --card-bg: #ffffff;
        --body-bg: #f8fafc;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Inter", Arial, sans-serif;
      }

      body {
        display: flex;
        background-color: var(--body-bg);
        height: 100vh;
        overflow: hidden;
        color: #333333;
      }

      /* Sidebar - Simple and Clean */
      .sidebar {
        width: 220px;
        background: var(--bg-dark);
        color: var(--text-light);
        display: flex;
        flex-direction: column;
        padding: 20px;
        position: fixed;
        height: 100vh;
        overflow-y: auto;
      }

      .logo {
        font-size: 20px;
        font-weight: 600;
        margin-bottom: 30px;
        color: var(--text-light);
        padding: 15px 0;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
      }

      .nav-item {
        color: var(--text-light);
        text-decoration: none;
        padding: 12px 15px;
        border-radius: 8px;
        margin-bottom: 8px;
        font-size: 15px;
        display: flex;
        align-items: center;
        gap: 12px;
        opacity: 0.8;
        transition: all 0.2s ease;
      }

      .nav-item:hover {
        background: rgba(255, 65, 108, 0.1);
        opacity: 1;
      }

      .nav-item.active {
        background: var(--primary);
        opacity: 1;
        font-weight: 500;
      }

      /* Main Content */
      main {
        flex: 1;
        margin-left: 220px;
        height: 100vh;
        overflow-y: auto;
        display: flex;
        flex-direction: column;
      }

      /* Header - Clean and Simple */
      .header {
        background: var(--primary);
        padding: 20px 30px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        position: sticky;
        top: 0;
        z-index: 100;
      }

      .welcome-section {
        color: var(--text-light);
      }

      .welcome-section h2 {
        font-size: 22px;
        font-weight: 600;
        margin-bottom: 5px;
      }

      .welcome-section .date-display {
        font-size: 14px;
        opacity: 0.9;
      }

      .user-info {
        display: flex;
        align-items: center;
        gap: 12px;
        color: var(--text-light);
      }

      .user-avatar {
        width: 40px;
        height: 40px;
        background: rgba(255, 255, 255, 0.9);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        color: var(--primary);
        font-weight: 600;
        font-size: 16px;
      }

      .user-details h4 {
        font-size: 14px;
        font-weight: 600;
        margin-bottom: 2px;
      }

      .user-details p {
        font-size: 12px;
        opacity: 0.8;
      }

      .content {
        padding: 25px;
        background-color: var(--body-bg);
        flex: 1;
      }

      /* Stats Grid - Simple Cards */
      .stats-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 20px;
        margin-bottom: 30px;
      }

      .stat-card {
        background: var(--card-bg);
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        border-left: 4px solid var(--primary);
      }

      .stat-card h3 {
        font-size: 28px;
        font-weight: 600;
        margin-bottom: 5px;
        color: #333333;
      }

      .stat-card p {
        color: #666666;
        font-size: 14px;
      }

      /* Content Grid */
      .content-grid {
        display: grid;
        grid-template-columns: 2fr 1fr;
        gap: 25px;
      }

      @media (max-width: 1024px) {
        .content-grid {
          grid-template-columns: 1fr;
        }
      }

      /* Cards */
      .card {
        background: var(--card-bg);
        border-radius: 10px;
        padding: 20px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        margin-bottom: 25px;
      }

      .card-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 20px;
        padding-bottom: 15px;
        border-bottom: 1px solid #eaeaea;
      }

      .card-header h2 {
        font-size: 16px;
        font-weight: 600;
        color: #333333;
      }

      .view-all {
        color: var(--primary);
        text-decoration: none;
        font-size: 14px;
        font-weight: 500;
      }

      .view-all:hover {
        text-decoration: underline;
      }

      /* Tables */
      table {
        width: 100%;
        border-collapse: collapse;
      }

      th {
        text-align: left;
        padding: 12px 0;
        border-bottom: 2px solid #eaeaea;
        color: #666666;
        font-weight: 600;
        font-size: 13px;
      }

      td {
        padding: 15px 0;
        border-bottom: 1px solid #f0f0f0;
        color: #333333;
      }

      tr:last-child td {
        border-bottom: none;
      }

      .student-info h4 {
        font-size: 14px;
        font-weight: 600;
        margin-bottom: 2px;
      }

      .student-info p {
        font-size: 12px;
        color: #666666;
      }

      .status-badge {
        padding: 4px 10px;
        border-radius: 4px;
        font-size: 12px;
        font-weight: 500;
        display: inline-block;
      }

      .status-present {
        background: rgba(16, 185, 129, 0.1);
        color: #10b981;
      }

      .status-absent {
        background: rgba(239, 68, 68, 0.1);
        color: #ef4444;
      }

      /* Quick Actions */
      .quick-actions {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 10px;
      }

      .action-btn {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 15px;
        background: #f8f9fa;
        border: 1px solid #eaeaea;
        border-radius: 8px;
        text-decoration: none;
        color: #333333;
        transition: all 0.2s ease;
      }

      .action-btn:hover {
        background: var(--primary);
        color: var(--text-light);
        border-color: var(--primary);
      }

      .action-btn span {
        font-size: 12px;
        font-weight: 500;
        text-align: center;
        margin-top: 5px;
      }

      /* Attendance Chart */
      .attendance-chart {
        padding: 15px 0;
      }

      .chart-bar {
        height: 8px;
        background: #eaeaea;
        border-radius: 4px;
        margin: 15px 0;
        overflow: hidden;
      }

      .chart-fill {
        height: 100%;
        background: var(--primary);
        border-radius: 4px;
        width: <%= stats.attendanceRate || 0 %>%;
      }

      .chart-info {
        display: flex;
        justify-content: space-between;
        font-size: 14px;
        color: #666666;
        margin-bottom: 10px;
      }

      /* Empty States */
      .empty-state {
        text-align: center;
        padding: 30px 20px;
        color: #999999;
      }

      .empty-state p {
        font-size: 14px;
      }

      /* Logout Link */
      .logout-link {
        margin-top: auto;
        padding-top: 20px;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
      }

      .logout-link a {
        color: var(--text-light);
        text-decoration: none;
        padding: 10px;
        display: flex;
        align-items: center;
        gap: 10px;
        opacity: 0.8;
      }

      .logout-link a:hover {
        opacity: 1;
      }

      /* System Status */
      .system-status-item {
        display: flex;
        justify-content: space-between;
        margin-bottom: 15px;
        font-size: 14px;
      }

      .system-status-item:last-child {
        margin-bottom: 0;
      }
    </style>
  </head>
  <body>
    <!-- Sidebar -->
    <div class="sidebar">
      <div class="logo">School Admin</div>

      <nav>
        <a href="/dashboard" class="nav-item active">
          <span>📊</span> Dashboard
        </a>
        <a href="/students" class="nav-item"> <span>👨‍🎓</span> Students </a>
        <a href="/teachers" class="nav-item"> <span>👩‍🏫</span> Teachers </a>
        <a href="/attendance" class="nav-item"> <span>📅</span> Attendance </a>
        <a href="/fees" class="nav-item"> <span>💰</span> Fees </a>
        <a href="/reports" class="nav-item"> <span>📈</span> Reports </a>
      </nav>

      <div class="logout-link">
        <a href="/auth/logout"> <span>🚪</span> Logout </a>
      </div>
    </div>

    <!-- Main Content -->
    <main>
      <!-- Header -->
      <div class="header">
        <div class="welcome-section">
          <h2>Welcome, <%= user.username %>!</h2>
          <div class="date-display">
            <%= new Date().toLocaleDateString('en-US', { weekday: 'long', year:
            'numeric', month: 'long', day: 'numeric' }) %>
          </div>
        </div>

        <div class="user-info">
          <div class="user-details">
            <h4><%= user.username %></h4>
            <p><%= user.role %></p>
          </div>
          <div class="user-avatar">
            <%= user.username.charAt(0).toUpperCase() %>
          </div>
        </div>
      </div>

      <!-- Content -->
      <div class="content">
        <!-- Stats Cards -->
        <div class="stats-grid">
          <div class="stat-card">
            <h3><%= stats.totalStudents || 0 %></h3>
            <p>Total Students</p>
          </div>

          <div class="stat-card">
            <h3><%= stats.totalTeachers || 0 %></h3>
            <p>Total Teachers</p>
          </div>

          <div class="stat-card">
            <h3><%= stats.attendanceRate || 0 %>%</h3>
            <p>Attendance Rate</p>
          </div>

          <div class="stat-card">
            <h3>
              KES <%= stats.totalFees ? stats.totalFees.toLocaleString() : '0'
              %>
            </h3>
            <p>Fees Collected</p>
          </div>
        </div>

        <!-- Main Content Grid -->
        <div class="content-grid">
          <!-- Left Column -->
          <div class="left-column">
            <!-- Recent Students -->
            <div class="card">
              <div class="card-header">
                <h2>Recent Students</h2>
                <a href="/students" class="view-all"> View all </a>
              </div>

              <% if (recentStudents && recentStudents.length > 0) { %>
              <table>
                <thead>
                  <tr>
                    <th>Name</th>
                    <th>Class</th>
                    <th>Status</th>
                    <th></th>
                  </tr>
                </thead>
                <tbody>
                  <% recentStudents.forEach(student => { %>
                  <tr>
                    <td>
                      <div class="student-info">
                        <h4><%= student.name %></h4>
                        <p>ID: #<%= student.id %></p>
                      </div>
                    </td>
                    <td><%= student.class || 'N/A' %></td>
                    <td>
                      <span class="status-badge status-present">Active</span>
                    </td>
                    <td>
                      <a href="/students/<%= student.id %>" class="view-all">
                        View
                      </a>
                    </td>
                  </tr>
                  <% }) %>
                </tbody>
              </table>
              <% } else { %>
              <div class="empty-state">
                <p>No students found</p>
              </div>
              <% } %>
            </div>

            <!-- Today's Absent Students -->
            <div class="card">
              <div class="card-header">
                <h2>Absent Today</h2>
                <a href="/attendance" class="view-all"> Mark attendance </a>
              </div>

              <% if (absentStudents && absentStudents.length > 0) { %>
              <table>
                <thead>
                  <tr>
                    <th>Name</th>
                    <th>Class</th>
                    <th>Status</th>
                  </tr>
                </thead>
                <tbody>
                  <% absentStudents.forEach(student => { %>
                  <tr>
                    <td>
                      <div class="student-info">
                        <h4><%= student.name %></h4>
                        <p>ID: #<%= student.id %></p>
                      </div>
                    </td>
                    <td><%= student.class || 'N/A' %></td>
                    <td>
                      <span class="status-badge status-absent">Absent</span>
                    </td>
                  </tr>
                  <% }) %>
                </tbody>
              </table>
              <% } else { %>
              <div class="empty-state">
                <p>All students present</p>
              </div>
              <% } %>
            </div>
          </div>

          <!-- Right Column -->
          <div class="right-column">
            <!-- Quick Actions -->
            <div class="card">
              <div class="card-header">
                <h2>Quick Actions</h2>
              </div>
              <div class="quick-actions">
                <a href="/students/new" class="action-btn">
                  <span>➕</span>
                  <span>Add Student</span>
                </a>
                <a href="/attendance" class="action-btn">
                  <span>📝</span>
                  <span>Mark Attendance</span>
                </a>
                <a href="/fees" class="action-btn">
                  <span>💰</span>
                  <span>Record Payment</span>
                </a>
                <a href="/reports" class="action-btn">
                  <span>📊</span>
                  <span>Generate Report</span>
                </a>
              </div>
            </div>

            <!-- Attendance Progress -->
            <div class="card">
              <div class="card-header">
                <h2>Attendance</h2>
              </div>
              <div class="attendance-chart">
                <div class="chart-info">
                  <span><%= stats.attendanceRate || 0 %>%</span>
                  <span
                    ><%= stats.todayAttendance || 0 %>/<%= stats.totalStudents
                    || 0 %></span
                  >
                </div>
                <div class="chart-bar">
                  <div class="chart-fill"></div>
                </div>
              </div>
            </div>

            <!-- System Status -->
            <div class="card">
              <div class="card-header">
                <h2>System Status</h2>
              </div>
              <div>
                <div class="system-status-item">
                  <span>Database</span>
                  <span>Online</span>
                </div>
                <div class="system-status-item">
                  <span>Storage</span>
                  <span>25% used</span>
                </div>
                <div class="system-status-item">
                  <span>Last Backup</span>
                  <span
                    ><%= new Date().toLocaleTimeString('en-US', { hour:
                    '2-digit', minute: '2-digit' }) %></span
                  >
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>
