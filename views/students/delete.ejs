<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Delete Student</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      }

      body {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 20px;
      }

      .confirm-container {
        background: white;
        border-radius: 15px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        width: 100%;
        max-width: 500px;
        overflow: hidden;
      }

      .confirm-header {
        background: linear-gradient(135deg, #ef4444, #dc2626);
        color: white;
        padding: 25px;
        text-align: center;
      }

      .confirm-header h1 {
        font-size: 24px;
        font-weight: 600;
      }

      .confirm-content {
        padding: 30px;
        text-align: center;
      }

      .warning-icon {
        font-size: 60px;
        color: #ef4444;
        margin-bottom: 20px;
      }

      .student-info {
        background: #fef2f2;
        border: 2px solid #fecaca;
        border-radius: 10px;
        padding: 20px;
        margin: 20px 0;
        text-align: left;
      }

      .student-info h3 {
        color: #374151;
        margin-bottom: 10px;
      }

      .student-info p {
        color: #6b7280;
        margin-bottom: 5px;
      }

      .warning-message {
        color: #ef4444;
        font-weight: 600;
        margin: 20px 0;
        padding: 15px;
        background: #fef2f2;
        border-radius: 8px;
      }

      .form-actions {
        display: flex;
        gap: 15px;
        margin-top: 30px;
      }

      .btn {
        flex: 1;
        padding: 14px;
        border: none;
        border-radius: 8px;
        font-size: 16px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        text-decoration: none;
        text-align: center;
      }

      .btn-danger {
        background: linear-gradient(135deg, #ef4444, #dc2626);
        color: white;
      }

      .btn-danger:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(239, 68, 68, 0.4);
      }

      .btn-cancel {
        background: #6b7280;
        color: white;
      }

      .btn-cancel:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(107, 114, 128, 0.4);
      }
    </style>
  </head>
  <body>
    <div class="confirm-container">
      <div class="confirm-header">
        <h1>🗑 Delete Student</h1>
      </div>

      <div class="confirm-content">
        <div class="warning-icon">⚠️</div>
        <h2>Are you sure you want to delete this student?</h2>

        <div class="student-info">
          <h3>Student Details:</h3>
          <p><strong>Name:</strong> <%= student.name %></p>
          <p><strong>Class:</strong> <%= student.class || 'N/A' %></p>
          <p>
            <strong>Guardian:</strong> <%= student.guardian_name || 'N/A' %>
          </p>
        </div>

        <div class="warning-message">
          ⚠️ Warning: This action cannot be undone! All student records,
          attendance, and exam data will be permanently deleted.
        </div>

        <form
          action="/students/delete/<%= student.id %>"
          method="POST"
          class="form-actions"
        >
          <a href="/students" class="btn btn-cancel">Cancel</a>
          <button type="submit" class="btn btn-danger">
            Yes, Delete Permanently
          </button>
        </form>
      </div>
    </div>
  </body>
</html>
